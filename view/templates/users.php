<ul class="list-inline" data-chunk="wrapper">
    <li class="list-inline-item mt-5 p-3 bg-white shadow rounded">
        <a class="card-link" href="/users/{{user_id}}">
            <h5>{{user_full_name}}</h5>
            <p class="text-muted font-weight-bold">{{role_name}}</p>
            <p class="text-muted">{{user_email}}</p>
        </a>
    </li>
</ul>